

--source suite/query_rewrite_plugins/include/have_plugin_prototype2_rewrite.inc

--replace_regex /\.dll/.so/

INSTALL PLUGIN prototype2_rewrite SONAME 'prototype2_rewrite.so';

SELECT 'Hello plugin' as message;

UNINSTALL PLUGIN prototype2_rewrite;
